import { describe, expect, it } from 'vitest';

describe('recoverRoomSessionHandler', () => {
  it('should fail cannot connect to redis player state', () => {
    expect(true).toEqual(true);
  });

  it('should fail no player state', () => {
    expect(true).toEqual(true);
  });

  it('should fail player state string unparsable', () => {
    expect(true).toEqual(true);
  });

  it('should fail cannot connect to redis game state', () => {
    expect(true).toEqual(true);
  });

  it('should fail no game state', () => {
    expect(true).toEqual(true);
  });

  it('should fail game state string unparsable', () => {
    expect(true).toEqual(true);
  });

  it('should send game state', () => {
    expect(true).toEqual(true);
  });
});
